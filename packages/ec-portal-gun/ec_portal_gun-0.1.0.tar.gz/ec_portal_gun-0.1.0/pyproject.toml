[tool.poetry]
name = "ec-portal-gun"
version = "0.1.0"
description = ""
authors = ["pysleuth <pysleuth@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
ec-portal-gun = "ec_portal_gun.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.12.3"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
