from setuptools import setup

VERSION = "0.0.1"

setup(
    name="pandas-deepscan-17",
    version=VERSION,
    description="",
    python_requires=">=3.6",
    install_requires=[],
    tests_require=[]
)
