[workspace]
members = ["wheel"]

[package]
name = "chik"
version = "0.6.0"
edition = "2021"
license = "Apache-2.0"
description = "A meta-crate that exports all of the Chik crates in the workspace."
authors = [
    "Richard Kiss <him@richardkiss.com>",
    "Arvid Norberg <arvid@chiknetwork.com>",
    "Brandon Haggstrom <b.haggstrom@chiknetwork.com>"
]
homepage = "https://github.com/Chik-Network/chik_rs"
repository = "https://github.com/Chik-Network/chik_rs"

[features]
default = [
    "bls",
    "client",
    "consensus",
    "protocol",
    "ssl",
    "traits",
    "wallet",
    "klvm-traits",
    "klvm-utils"
]

bls = ["dep:chik-bls"]
client = ["dep:chik-client"]
consensus = ["dep:chik-consensus"]
protocol = ["dep:chik-protocol"]
ssl = ["dep:chik-ssl"]
traits = ["dep:chik-traits"]
wallet = ["dep:chik-wallet"]
klvm-traits = ["dep:klvm-traits"]
klvm-utils = ["dep:klvm-utils"]

[dependencies]
chik-bls = { path = "./crates/chik-bls", optional = true }
chik-client = { path = "./crates/chik-client", optional = true }
chik-consensus = { path = "./crates/chik-consensus", optional = true }
chik-protocol = { path = "./crates/chik-protocol", optional = true }
chik-ssl = { path = "./crates/chik-ssl", optional = true }
chik-traits = { path = "./crates/chik-traits", optional = true }
chik-wallet = { path = "./crates/chik-wallet", optional = true }
klvm-traits = { path = "./crates/klvm-traits", optional = true }
klvm-utils = { path = "./crates/klvm-utils", optional = true }

[profile.release]
lto = "thin"

# This is also necessary in `wheel/Cargo.toml` to make sure the `wheel` crate builds as well.
# Pin the `blst` dependency to the correct revision, since the fix has not been properly released yet.
[patch.crates-io]
blst = { git = "https://github.com/supranational/blst.git", rev = "0d46eefa45fc1e57aceb42bba0e84eab3a7a9725" }
