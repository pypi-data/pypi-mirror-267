[project]
name = "gnu-screen-class"
version = "1.2"
dependencies = []
description = "wrapper around gnu screen"
readme = "README.md"
license = { file = "LICENSE" }
requires-python= ">= 3.8"
authors = [
    {name = "Gerard"},
    {email = "gweatherby@uchc.edu"}
]

[project.urls]
Homepage = "https://github.com/NMRhub/gnuscreen"

[project.scripts]
gnuscreen = "gnuscreen.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

