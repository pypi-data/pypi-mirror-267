[tool.poetry]
name = "aixblock_ml"
version = "0.0.6"
description = ""
authors = ["TonyShark <quoi@wow-ai.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project.scripts]
aixblock-ml = "aixblock_ml.server:main"
[options.entry_points]
console_scripts = {aixblock-ml= "aixblock_ml.server:main"}