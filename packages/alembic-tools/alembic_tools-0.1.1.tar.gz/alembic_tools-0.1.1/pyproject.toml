[tool.poetry]
name = "alembic-tools"
version = "0.1.1"
description = "Tools anhanced alembic model"
authors = ["Smairon <man@smairon.ru>"]
readme = "README.md"
packages = [{ include = "alembic_tools" }]

[tool.poetry.dependencies]
python = "^3.12"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"