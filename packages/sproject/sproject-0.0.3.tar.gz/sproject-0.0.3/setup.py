from setuptools import setup, find_packages

setup(
    name='sproject',  # Replace with your desired package name
    version='0.0.3',  # Version number (update for future releases)
    description='A sample Python package for demonstration',
    author='Grand Master',
    packages=find_packages(),  # Automatically finds your package
    install_requires=[]  # Add any external dependencies here (if needed)
)
