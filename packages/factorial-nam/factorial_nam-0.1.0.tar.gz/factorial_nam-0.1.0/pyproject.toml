[tool.poetry]
name = "factorial-nam"
version = "0.1.0"
description = ""
authors = ["nam-hoang-le <lenam1072004@gmail.com>"]
readme = "README.md"
packages = [{include = "factorial_nam"}]

[tool.poetry.dependencies]
python = "^3.10"
streamlit = "^1.33.0"


[tool.poetry.group.test.dependencies]
pytest = "^8.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
