[tool.poetry]
name = "pylaptoppred"
version = "0.1.0"
description = "prediccting laptop prices base on algorithms"
authors = ["Lucas Liu"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
statsmodels = "^0.14.1"
seaborn = "^0.13.2"
pandas = "^2.2.0"
numpy = "^1.26.4"
altair = "^5.2.0"
imbalanced-learn = "^0.12.0"
matplotlib = "^3.8.3"
scikit-learn = "^1.4.1"
tabulate = "^0.9.0"
click = "^8.1.7"
pillow = "^10.2.0"
ipython = "^8.20.0"
vl-convert-python = "^1.3.0"



[tool.poetry.dev-dependencies]

[tool.poetry.group.dev.dependencies]
pytest = "^8.1.1"
pytest-cov = "^5.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
