[build-system]
requires = [ "setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "leidian-helper"
version = "0.0.2"
requires-python = ">=3.10"
dependencies = [
]

#[project.scripts]

