[build-system]
requires = [ "setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "leidian-helper"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "mypy",
    "tqdm",
    "pyyaml",
    "dacite",
    "retrying"
]

#[project.scripts]

