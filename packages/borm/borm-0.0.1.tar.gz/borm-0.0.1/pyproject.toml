[tool.poetry]
name = "borm"
version = "0.0.1"

description = "Simple lightweight Django-like ORM for SQLite"
authors = ["Baran Erfani <erfani.baran@gmail.com>"]
license = "GPLv3"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6.1"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
