[tool.poetry]
name = "test-option-deps"
version = "3.0.0"
description = "Testing optional dependencies"
authors = ["Saipraneeth <2506664+msaipraneeth@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
smart-open = [
	{extras = ["all"], version = "^7.0.4", optional = true},
	{version = "^7.0.4"}
]

[tool.poetry.scripts]
test-option-deps = 'test_option_deps:cli'

[tool.poetry.extras]
all = ['smart-open']

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
