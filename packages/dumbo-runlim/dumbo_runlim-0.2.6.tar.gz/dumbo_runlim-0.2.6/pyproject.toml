[tool.poetry]
name = "dumbo-runlim"
version = "0.2.6"
description = "A CLI to run and limit processes (and their subprocesses)"
authors = ["Mario Alviano <mario.alviano@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
dumbo-utils = "^0.3.2"
psutil = "^5.9.7"
gitpython = "^3.1.42"


[tool.poetry.dev-dependencies]
pytest = "^8.1.1"
coverage = "^7.3.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
