from setuptools import setup, find_packages

setup(
    name='egypt',
    version='0.0.1',
    description='A small example packag for pyramid',
    packages=find_packages(),
    python_requires='>=3.6',
)
