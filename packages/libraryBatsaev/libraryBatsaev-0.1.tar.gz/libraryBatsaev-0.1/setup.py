# setup.py

from setuptools import setup, find_packages

setup(
    name='libraryBatsaev',
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # Укажите здесь зависимости, если они есть
)
