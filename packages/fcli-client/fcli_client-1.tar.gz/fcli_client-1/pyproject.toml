[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fcli-client"
version = "1.0.0"
dependencies = [
    "bs4",
    "lxml",
    "pandas",
    "scipy",
    "requests",
]
