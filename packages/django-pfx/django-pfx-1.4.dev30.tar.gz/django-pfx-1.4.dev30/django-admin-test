#!/bin/bash
# Run django-admin in test module with test settings.
#
# To generate/update .po run:
# ./django-admin-test makemessages -a
# To compile i18n run:
# ./django-admin-test compilemessages

DJANGO_SETTINGS_MODULE=tests.settings.dev
(cd ./tests && django-admin "$@")
