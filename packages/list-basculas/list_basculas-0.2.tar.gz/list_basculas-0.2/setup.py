from setuptools import setup, find_packages

setup(
    name='list_basculas',
    version='0.2',
    packages=find_packages(),
    install_requires=[
        'Django',
        # Add any other dependencies here
    ],
)
